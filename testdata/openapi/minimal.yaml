openapi: 3.0.0
info:
  title: Minimal
  version: 1.0.0
paths:
  /health:
    get:
      summary: Health check
  /accounts/{id}:
    delete:
      summary: Delete account
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
